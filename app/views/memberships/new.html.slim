.container
  .row
    .col-md-9
      .well
        = simple_form_for @membership, { class: 'form-horizontal' } do |f|
          .form-group
            = f.association :student, label: t('simple_form.labels.membership.student_id')
          .form-group
            = f.association :course, label: t('simple_form.labels.membership.course_id')
          .panel-footer
            = f.submit